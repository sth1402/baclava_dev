% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Class_Strategy.R
\name{MH}
\alias{MH}
\alias{is.MH}
\title{Define a single MH update}
\usage{
MH(
  params,
  s0 = 1,
  adaptive = adapt(),
  L = diag(nrow = length(params), ncol = length(params)),
  D = rep(1, length(params))
)

is.MH(x)
}
\arguments{
\item{params}{Character vector of parameter names included in the MH
(duplicates are removed).}

\item{s0}{Numeric scalar; initial step-size scale for MH
(step is \eqn{s \cdot (L z)}).}

\item{adaptive}{An Adapt object.}

\item{L}{Numeric matrix (length(params)xlength(params)) giving the initial
settings of the covariance matrix. Defaults to identity matrix.}

\item{D}{Numeric vector (length(params)) giving the initial
settings of the diagonal shape matrix. Defaults to 1s.}
}
\value{
An R6 \code{MH} object (class \code{"MH"}) with a stable
  \code{$toList()} representation suitable for downstream C++/Rcpp code.
}
\description{
Convenience constructor for a \code{MH} (R6) describing one MCMC MH update
step. Use this to specify which parameters are updated together, plus 
optional adaptation settings.
}
\section{Functions}{
\itemize{
\item \code{is.MH()}: Inheritance test

}}
\examples{
# MH over two parameters with default adaptation
b1 <- MH(params = c("mu", "shape"))

# MH with custom step-size and defaultadaptation
b2 <- MH(
  params = c("mu", "shape"),
  s0 = 0.8,
  adaptive = adapt())

}
